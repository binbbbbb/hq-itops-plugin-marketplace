# API Token Automation

Runs the fixed Postman Bearer-token refresh workflow for IT Service, Zeus, RCM, the MACM folder, and ACM excluding vCenter.

Set `POSTMAN_API_KEY` in the environment, or create an untracked `postman-api-key.txt` in this directory before running the workflow.
