interface:
  display_name: "Phishing Email Screening"
  short_description: "Run fixed Coremail metadata phishing screening"
  default_prompt: "Use $phishing-email-screening to scan the fixed Coremail source with its local allowlist and summarize the local report."

policy:
  allow_implicit_invocation: true
